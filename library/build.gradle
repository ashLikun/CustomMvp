apply plugin: 'com.android.library'

android {
    compileSdkVersion rootProject.ext.compileSdkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion
    dataBinding {
        enabled true
    }
    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode rootProject.ext.versionCode
        versionName rootProject.ext.versionName
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    repositories {
        flatDir {
            dirs 'libs'
        }
        maven {
            url "https://jitpack.io"
        }
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation rootProject.ext.v4Version
    implementation rootProject.ext.appcompat
    compileOnly 'com.github.ashLikun.frame:supertoobar:' + rootProject.ext.frameVersion
    compileOnly 'com.github.ashLikun:XRecycleView:1.1.5'
    compileOnly 'com.github.ashLikun.CommonAdapter:databindadapter:1.1.9'

    compileOnly 'com.github.ashLikun:CommonUtils:1.3.4'
    compileOnly 'com.github.ashLikun:CustomDialog:1.1.1'
    compileOnly 'com.github.ashLikun:LoadSwitch:1.0.4'
    compileOnly 'com.github.ashLikun:OkHttpUtils:1.4.2'
}

apply from: '../maven.gradle'